deploy:
    type: deploy
    script:
        - jekyll build
        - scp -r -o UserKnownHostsFile=/keys/known_hosts -i /keys/opacweb_deploy _site/* opacweb@smedry.rami.io:html/
    only:
        - master
